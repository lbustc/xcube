;
; XCUBE expiries configuration
;

[general]
; Specify the server verbosity level.
; This can be one of: debug, info, notice and warning.
log_level=info
; Specify the front IP address.
front_ip=
; Specify the front port.
front_port=
; account
account=
; password
passwd=

